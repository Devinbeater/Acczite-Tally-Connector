<Window x:Class="Acczite20.Views.CustomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Acczite System Message" 
        Height="Auto" Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">

    <Border Background="White" 
            CornerRadius="20" 
            BorderBrush="#E2E8F0" 
            BorderThickness="1.5"
            Margin="20"
            Padding="24">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Opacity="0.15" Direction="270" ShadowDepth="5"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Icon Header -->
            <Border x:Name="BorderIcon" Width="56" Height="56" CornerRadius="28" Background="#FEF2F2" HorizontalAlignment="Left" Margin="0,0,0,20">
                <TextBlock x:Name="TxtIcon" FontFamily="Segoe MDL2 Assets" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Text="&#xE7BA;"/>
            </Border>

            <!-- Text Content -->
            <TextBlock Grid.Row="1" x:Name="TxtTitle" Text="Title" FontSize="20" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
            <TextBlock Grid.Row="2" x:Name="TxtMessage" Text="Message goes here." FontSize="14" Foreground="#64748B" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,28"/>

            <!-- Actions -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnSecondary" Content="Cancel" Click="Secondary_Click" Style="{StaticResource GhostButtonStyle}" Height="40" Padding="20,0" Margin="0,0,12,0" Visibility="Collapsed"/>
                <Button x:Name="BtnPrimary" Content="OK" Click="Primary_Click" Style="{StaticResource PrimaryButtonStyle}" Height="40" Padding="32,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
