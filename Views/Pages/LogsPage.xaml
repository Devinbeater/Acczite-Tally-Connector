<Page x:Class="Acczite20.Views.Pages.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      mc:Ignorable="d"
      Title="System Logs"
      Background="{StaticResource LightTealBg}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Back Navigation -->
        <Button Content="← Back" Click="Back_Click" 
                Style="{StaticResource SidebarButtonStyle}" Foreground="{StaticResource PrimaryTeal}" 
                HorizontalAlignment="Left" Height="32" Margin="0,0,0,6" FontSize="13"/>

        <!-- Header -->
        <TextBlock Grid.Row="1" Text="System Logs &amp; Events"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource PrimaryTeal}"
                   Margin="0,0,0,5" />
                   
        <TextBlock Grid.Row="2" 
                   Text="Real-time trace of synchronization events, errors, and system status."
                   FontSize="14"
                   Foreground="{StaticResource CharcoalText}"
                   Opacity="0.7"
                   Margin="0,0,0,25" />

        <!-- Log Panel -->
        <Border Grid.Row="3" Background="{StaticResource BaseWhite}" CornerRadius="12" Padding="24" 
                BorderBrush="#E5E7EB" BorderThickness="1"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Tools & Filters -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Button Content="Refresh" Width="100" Style="{StaticResource SidebarButtonStyle}" Foreground="{StaticResource PrimaryTeal}"/>
                    <Button Content="Export CSV" Width="100" Style="{StaticResource SidebarButtonStyle}" Foreground="{StaticResource PrimaryTeal}" Margin="10,0,0,0"/>
                    <Button Content="Clear Logs" Width="100" Style="{StaticResource SidebarButtonStyle}" Foreground="DarkRed" Margin="10,0,0,0"/>

                    <Border Background="#F9FAFB" CornerRadius="6" BorderBrush="#E5E7EB" BorderThickness="1" Margin="20,0,0,0" Padding="10,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource CharcoalText}" Opacity="0.7"/>
                            <ComboBox Width="120" BorderThickness="0" Background="Transparent" SelectedIndex="0">
                                <ComboBoxItem Content="All Events"/>
                                <ComboBoxItem Content="Errors Only"/>
                                <ComboBoxItem Content="Sync Success"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- The DataGrid / ListBox for logs -->
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Monitor.Logs}"
                          AutoGenerateColumns="False" 
                          HeadersVisibility="Column" 
                          Background="Transparent" 
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F3F4F6"
                          RowBackground="Transparent"
                          AlternatingRowBackground="#F9FAFB"
                          IsReadOnly="True">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F3F4F6"/>
                            <Setter Property="Foreground" Value="{StaticResource CharcoalText}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="10,12"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#E5E7EB"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,12"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="160"/>
                        <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="100"/>
                        <DataGridTextColumn Header="Module" Binding="{Binding Module}" Width="150"/>
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>
